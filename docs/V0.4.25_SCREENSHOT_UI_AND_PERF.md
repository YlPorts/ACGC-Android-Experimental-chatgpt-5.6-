# v0.4.25 Screenshot UI + performance pass

Fixes English baked UI confirmed by user screenshots:
- outdoor HUD weekday + AM/PM family (clk_win_*, distinct from tim_win calendar textures)
- room Camera / Lights
- diary Entry / latest entry / Write / Quit
- inventory Items / Letters / Bells
- collection Insects / Fish
- melody e-Reader label

Performance:
- ui_strings.ini entries are sorted once at load and pc_language_ui_lookup() now uses binary search
  instead of scanning every entry linearly.

ES/FR/DE/IT graphical variants are generated for the port and preserve exact source formats/sizes.
