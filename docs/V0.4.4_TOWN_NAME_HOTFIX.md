# v0.4.4 Town-name hotfix

- PC/Android no longer reads string slot 484 when formatting town names.
- Fixes custom names such as `latam` becoming `latammariposa` in translated dialogue.
- The fix no longer depends on external language-pack load state.
- Saved town names are not modified.
- MultiLanguage EN/ES/FR/DE/IT support from v0.4.3 is retained.
- No City Folk / CityExpansion changes are included.
