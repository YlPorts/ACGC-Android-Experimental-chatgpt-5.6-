# v0.4.18 cumulative settings/keybindings completion

Includes everything in v0.4.17 plus:
- Settings title and labels use ui_strings.ini on PC and Android.
- Display-mode, quality, texture-loading, Resetti, shop-visit, touch and other values are pack-owned.
- Resolution confirmation/revert countdown is pack-owned.
- Discard-changes confirmation is pack-owned.
- Keybindings title, columns, directional action labels, reset/defaults and capture hints are pack-owned.
- Existing internal fallback labels remain in code but are no longer the displayed language source.
- No save filenames, Memory Card identifiers, debug menus or message-ID comments were translated.
- No City Folk / CityExpansion content.

Audit note:
The remaining English-looking literals are now overwhelmingly:
1. internal fallback labels,
2. developer/debug screens,
3. comments describing message IDs,
4. Memory Card metadata/filenames where changing text can affect cross-language compatibility,
5. graphical artwork still awaiting verified per-language binary replacements.
